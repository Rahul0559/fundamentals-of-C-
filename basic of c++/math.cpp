#include <iostream>
#include<cmath>
using namespace std;
int main(){
 int x =2;
 int y =9;
 int z;
 //to find max num
 z = max(x,y);
 cout<<z<<endl;
// to find min num
 z = min(x,y);
 cout<<z<<endl;
 //to find pow to any num
 z =pow(2,8);
 cout<<z<<endl;
 //to find squre root of any number
 z = sqrt(9);
 cout<<z<<endl;
 // to find absulte value
 z= abs(-9);
 cout<<z<<endl;
 //to find roud value of any num
 z = round(9.8);
 cout<<z<<endl;
 // to fil ciel value
 z = ceil(x);
 cout<<z<<endl;
 // to find remainderof a value
 z =floor(x);
 cout<<z<<endl;
 return 0;
}